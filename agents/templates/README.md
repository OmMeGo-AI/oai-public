# Sub-Agent Templates

This folder will contain reusable Markdown templates for sub-agents.

Planned contents:
- minimal sub-agent templates
- tool-using sub-agent templates
- supervisor or coordinator templates
